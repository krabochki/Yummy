const source = 'http://127.0.0.1:3000/';

export const usersUrl = source + 'users';
export const recipesUrl = source + 'recipes';
export const sectionsUrl = source + 'sections';
export const categoriesUrl = source + 'categories';

export const plansUrl = source + 'plans';